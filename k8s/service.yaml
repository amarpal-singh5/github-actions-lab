apiVersion: v1
kind: Service
metadata:
  name: kubernetes-gitops-platform-service
spec:
  selector:
    app: kubernetes-gitops-platform
  ports:
    - port: 80
      targetPort: 5000
  type: NodePort
